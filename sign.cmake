FIND_PACKAGE(PythonInterp 3 REQUIRED)
IF(APPLE)
  EXECUTE_PROCESS(COMMAND "${PYTHON_EXECUTABLE}" "${FASTOGT_CMAKE_MODULES_DIR}/scripts/fastogtsign.py" $ENV{SIGNING_IDENTITY} "${EXECUTABLE_PATH}")
ELSE()
  EXECUTE_PROCESS(COMMAND "${PYTHON_EXECUTABLE}" "${FASTOGT_CMAKE_MODULES_DIR}/scripts/fastogtsign.py" FastoGT "${EXECUTABLE_PATH}")
ENDIF(APPLE)